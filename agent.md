# Agent Instructions For Fix Intake

このリポジトリでは、修正依頼の受付と実装を分けて運用します。

## 参照先

- 修正バックログの正本は `docs/FIX_BACKLOG.md` です。
- このファイルは、このチャット以外から「ToDoを見て修正して」と依頼された場合も参照します。

## 基本ルール

- 新しい修正点を受け取ったら、すぐにコードを直さず、まず `docs/FIX_BACKLOG.md` に整理して追加します。
- ユーザーが明示的に「まとめて修正して」「このToDoを見て修正して」と指示したときだけ、バックログを基準に実装へ進みます。
- 修正依頼を追加するときは、カテゴリ、優先度、状態、要約、再現条件または期待結果、影響範囲、補足メモを残します。
- 同じ問題や近い問題がある場合は、新規項目を乱立させず、既存項目へ統合または関連付けします。
- 画像つき指摘は、画像ファイルへの参照だけで済ませず、どの画面のどの要素にどんな問題があるかを文章で詳細に残します。
- 添付画像を残す必要がある場合は、画像を `docs/fix_backlog_assets/` に保存し、バックログ項目の Notes に保存先を記録します。
- 完了したタスクは `Done` に更新したあと、バックログの通常一覧に残し続けず `Archived` セクションへ自動的に移動します。

## 優先度の既定

- `P0`: 進行不能、重大破損、データ損失リスク
- `P1`: 主要機能の不具合、優先修正
- `P2`: 通常の改善、使い勝手修正
- `P3`: 軽微な文言、見た目、将来候補

## 状態の既定

- `Open`
- `Ready`
- `In Progress`
- `Done`
- `Blocked`

## 受付時の流れ

1. 依頼内容を要約する。
2. 既存のバックログに重複または関連項目がないか確認する。
3. もっとも近いカテゴリと優先度を付ける。
4. 実装に必要な背景を、画像内容も含めて文章で残す。
5. 必要なら添付画像を `docs/fix_backlog_assets/` へ保存する。
6. バックログの該当セクションを更新する。

## 実装開始条件

- ユーザーが明示的に修正実行を依頼していること。
- 実装対象が `docs/FIX_BACKLOG.md` に記録されていること。
- 必要なら着手前に項目の状態を `Ready` へ見直すこと。
